
# This file is used by ESLint to determine which files and directories to ignore.
# It is not meant to be edited manually, but rather generated by the build process.
# Ignore build output directories
# Ignore Jest/CRA tests for lint (we’ll re‑enable when tests are configured)
src/**/*.test.js

